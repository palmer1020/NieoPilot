# 定时任务逻辑检查文档

## 当前定时任务的执行流程

### 1. 定时任务启动
- 等待到指定时间（`scheduled_datetime`）
- 检查是否有 `wild_capture` 任务正在运行

### 2. 如果有正在运行的捕捉任务
- **等待安全打断时机**：
  - 如果正在战斗，等待战斗结束
  - 如果正在恢复，等待恢复完成
  - 等待进入稳态扫描阶段（`_is_scanning_steady_state`）
- **打断捕捉任务**：
  - 设置 `stop_current = True`
  - 执行"回到基地.json"脚本
  - 重置 `stop_current = False`

### 3. 执行定时任务的脚本序列
- **步骤1**：根据 `scheduled_from_hangup` 标志：
  - 如果 `True`：执行"回到基地.json"
  - 如果 `False`：执行"登录.json"
- **步骤2**：执行切换脚本（`toXXX.json`）
  - 根据 `wild_capture_profile` 选择：
    - "mantis" -> "to螳螂"
    - "dugulu" -> "to嘟咕噜"
    - "shuangta" -> "to双塔"
    - **"xiaodouya" -> "to小豆芽"** ✅ 已支持
- **步骤3**：等待进入目标地图
  - 调用 `dar_route_runner._wait_for_map_ready(profile, stop_event)`
  - 等待 `/resource/map/{profile.map_swf_id}.swf` + `newNpc` 信号
- **步骤4**：启动捕捉任务
  - 调用 `dar_route_runner.run(...)`

## 用户测试场景分析

### 用户想要测试的场景
1. **先启动小豆芽捕捉模式**（`wild_capture` 任务）
   - 这会调用 `dar_route_runner.run()`
   - 它会先等待进入地图（`_wait_for_map_ready`）
2. **然后设置定时任务**，在某个时间点执行：
   - 登录.json
   - to小豆芽.json

### 当前逻辑如何处理
1. 如果用户在启动捕捉任务**之前**设置定时任务：
   - ✅ 定时任务会在指定时间执行登录+to小豆芽脚本
   - ✅ 然后等待进入地图
   - ✅ 然后启动捕捉任务
   - **这是正确的流程**

2. 如果用户在启动捕捉任务**之后**设置定时任务（用户描述的场景）：
   - ⚠️ 定时任务会检测到 `wild_capture` 正在运行
   - ⚠️ 会等待捕捉任务进入稳态扫描阶段
   - ⚠️ 然后打断捕捉任务，执行"回到基地.json"
   - ⚠️ 然后执行登录+to小豆芽脚本
   - ⚠️ 最后启动新的捕捉任务
   - **但用户可能期望的是：捕捉任务已经启动并等待地图，定时任务只需要执行登录+to小豆芽脚本**

## 潜在问题

### 问题1：捕捉任务已在等待地图时
如果捕捉任务已经启动并正在等待进入地图（`_wait_for_map_ready`），定时任务会：
- 等待捕捉任务进入稳态扫描阶段（但此时还在等待地图，不会进入稳态扫描）
- 超时后强制打断
- 执行回到基地脚本（但用户可能不想执行回到基地）
- 然后执行登录+to小豆芽脚本

### 问题2：逻辑矛盾
用户说"先执行小豆芽捕捉启动到地图启动"，这意味着：
- 捕捉任务已经启动
- 正在等待进入地图
- 但此时还没有执行登录和to小豆芽脚本

这个场景在逻辑上是矛盾的，因为：
- 如果还没有执行登录和to小豆芽脚本，游戏可能不在正确的位置
- 捕捉任务无法进入正确的地图

## 建议的正确流程

### 场景A：定时任务在捕捉任务之前设置
1. 用户设置定时任务（选择xiaodouya，设置时间）
2. 定时任务到达指定时间
3. 执行登录.json
4. 执行to小豆芽.json
5. 等待进入地图
6. 启动捕捉任务

### 场景B：捕捉任务已在运行，定时任务需要打断
1. 用户已经启动捕捉任务
2. 用户设置定时任务
3. 定时任务到达指定时间
4. 检测到捕捉任务正在运行
5. 等待安全打断时机（稳态扫描阶段）
6. 打断捕捉任务
7. 执行回到基地.json（或登录.json，取决于from_hangup）
8. 执行to小豆芽.json
9. 等待进入地图
10. 启动捕捉任务

## 代码检查结果

### ✅ 已正确实现
- [x] xiaodouya profile 支持
- [x] "to小豆芽" 脚本名称映射
- [x] 定时任务的基本流程
- [x] 捕捉任务的打断逻辑

### ⚠️ 需要注意
- 如果捕捉任务正在等待地图（还未进入稳态扫描），定时任务会等待超时（最多5分钟）
- 如果用户想要"先启动捕捉，然后执行登录+to小豆芽"，可能需要调整逻辑

## 测试建议

1. **测试场景1**：先设置定时任务，然后等待执行
   - 设置定时任务（选择xiaodouya）
   - 等待定时时间到达
   - 验证是否执行：登录.json -> to小豆芽.json -> 等待地图 -> 启动捕捉

2. **测试场景2**：先启动捕捉，然后设置定时任务
   - 先手动启动小豆芽捕捉
   - 等待进入地图并进入稳态扫描
   - 设置定时任务
   - 等待定时时间到达
   - 验证是否安全打断并重新执行脚本序列






