# config.py.template
# 
# 使用说明：
# 1. 复制此文件为 config.py：cp config.py.template config.py (Linux/Mac) 或 copy config.py.template config.py (Windows)
# 2. 根据你的环境修改下面的配置项
# 3. config.py 已在 .gitignore 中，不会被上传到 Git

# 1. 游戏窗口标题
WINDOW_TITLE = "nieo" 

# 2. 游戏启动路径
# 修改为你的游戏可执行文件路径
GAME_PATH = r"C:\Users\你的用户名\AppData\Local\Programs\nieo\nieo.exe"

# 3. 脚本逻辑分辨率 (写脚本时用的坐标系，保持 1200x700 不变，方便写逻辑)
GAME_LOGIC_W = 1200
GAME_LOGIC_H = 700

# 4. ★★★ 真实测量比例 (系统锚点) ★★★
# 这是你测量出来的"黄金尺寸"，通常与逻辑分辨率相同
REAL_WIDTH = 1200
REAL_HEIGHT = 700
# 计算出固定的宽高比
FIXED_RATIO = REAL_WIDTH / REAL_HEIGHT 

# 5. 项目路径配置（通常不需要修改，除非项目结构特殊）
import os
BASE_PATH = os.path.dirname(os.path.abspath(__file__))
ASSETS_PATH = os.path.join(BASE_PATH, "assets")
REGIONS_PATH = os.path.join(ASSETS_PATH, "regions")
TEMPLATES_PATH = os.path.join(ASSETS_PATH, "templates")
FIX_SCRIPT_PATH = os.path.join(BASE_PATH, "fix_script")

# 6. Tesseract OCR 路径配置
# 修改为你的 Tesseract OCR 安装路径
TESSERACT_CMD = r"C:\Program Files\Tesseract-OCR\tesseract.exe"
TESSDATA_PREFIX = r"C:\Program Files\Tesseract-OCR\tessdata"

# 7. 游戏资源文件路径配置
# 游戏资源文件夹路径（用于 SWF 文件操作）
# 修改为你的游戏资源文件夹路径
GAME_ASSET_BASE_PATH = r"C:\Users\你的用户名\AppData\Local\Programs\nieoasset"
GAME_SWF_FOLDER = os.path.join(GAME_ASSET_BASE_PATH, "resource", "pet", "swf")
GAME_SWF_OG_FOLDER = os.path.join(GAME_ASSET_BASE_PATH, "resource", "pet", "swf_og")
GAME_SWF_OG_TEMPLATE = os.path.join(GAME_SWF_OG_FOLDER, "254.swf")  # 用于补齐缺失文件的模板

# 8. 项目数据文件夹路径（相对于项目根目录，通常不需要修改）
HV_SAMPLES_PATH = os.path.join(BASE_PATH, "hv_samples")
BASELINE_DATA_PATH = os.path.join(BASE_PATH, "baseline_data")

# ===============================================================
# 9. 日常任务执行顺序
# ===============================================================
# 这里填写你在 fix_script 文件夹里录好的文件名 (不需要 .json 后缀)

DAILY_SEQUENCE = [
     "A","B","C","D","E","F"
]

DAILY_SEQUENCE0 = [
    "登录"
]

